{
  "dependencies": {
    "node-sass": "^4.14.1",
    "sass": "^1.26.10"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "sass --watch scss:css --style compressed",
    "start": "npm start"
  }
}
